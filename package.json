{
  "name": "orb",
  "version": "0.3.0",
  "description": "Newtonian solar-system simulation that shows the solar system has no center.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "deploy": "vite build && gh-pages -d dist",
    "init": "python3 index.py"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "puppeteer-core": "^23.11.1",
    "typescript": "^5.6.3",
    "vite": "^6.0.0"
  }
}